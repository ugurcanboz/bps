# V5.1.0 Cloud Highscore Setup

Cloud Highscore ist weiterhin vorbereitet, aber nicht zwingend für den Offline-Betrieb erforderlich.
Die PWA funktioniert lokal über IndexedDB/localStorage. Für Cloud-Betrieb muss die Konfiguration in `data/cloud-config.js` gesetzt werden.
