'use strict';
const assert=require('assert');
const fs=require('fs');
const path=require('path');
const root=path.resolve(__dirname,'..');
const read=f=>fs.readFileSync(path.join(root,f),'utf8');
let n=0;function ok(name,fn){fn();n++;console.log('✓',name);}
const fn=read('functions/index.js');
const auth=read('js/modules/egt-auth-engine.js');
const local=read('functions/tools/bootstrap-admin.mjs');
ok('release version advanced to G54.50.2G',()=>assert(read('js/core/app-config.js').includes("var VERSION = 'G54.50.2G'")));
ok('failed remote bootstrap is no longer part of frontend',()=>assert(!auth.includes('registerBootstrapAdmin')));
ok('remote bootstrap callable is not shipped',()=>assert(!fn.includes('exports.bootstrapAdmin')));
ok('no App Check bypass remains',()=>assert(!fn.includes('enforceAppCheck:false')));
ok('local bootstrap validates service account type',()=>assert(local.includes("serviceAccount.type!=='service_account'")));
ok('local bootstrap checks existing global completion lock',()=>assert(local.includes("lock.status==='completed'")));
ok('same administrator can be repaired idempotently',()=>assert(local.includes("completedByEmail")));
ok('another existing administrator is blocked',()=>assert(local.includes('existiert bereits ein anderes Admin-Konto')));
ok('private key path is supplied only at runtime',()=>assert(local.includes('NOVURA_SERVICE_ACCOUNT_PATH')));
ok('service worker invalidates previous release',()=>{const sw=read('service-worker.js');assert(sw.includes('G54.50.2G-2026-07-13'));assert(sw.includes('egt-trainer-g54-50-2g'));});
console.log(`G54.50.2F→G Local Bootstrap Security Regression: ${n}/10 bestanden`);
