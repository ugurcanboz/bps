const fs=require('fs'),path=require('path'),cp=require('child_process');const root=path.resolve(__dirname,'..'),checks=[];const check=(n,v)=>{checks.push({n,ok:!!v});console.log((v?'PASS ':'FAIL ')+n)};cp.execFileSync(process.execPath,[path.join(root,'tools/rebranding-audit.mjs'),root],{stdio:'pipe'});const r=JSON.parse(fs.readFileSync(path.join(root,'rebranding','G54.48.4-final-scan.json'),'utf8'));check('Version G54.48.4 oder neuer',/var VERSION = 'G54\.(?:4[8-9]|5[0-9])\./.test(fs.readFileSync(path.join(root,'js/core/app-config.js'),'utf8')));check('Keine aktiven BPS/CTC-Inhaltstreffer',r.summary.activeContentHits===0);check('Keine aktiven BPS/CTC-Dateinamen',r.summary.activeFilenameHits===0);check('Keine Alias-Schicht',!fs.existsSync(path.join(root,'js/core/novura-legacy-aliases.js')));for(const f of ['js/core/ctc-admin-report-engine.js','js/core/ctc-lohr-exam-structure-engine.js','js/core/ctc-simulation-stability-engine.js','js/modules/ctc-flowlogic-adapter.js','css/ctc-flowlogic-integration.css','css/ctc-lohr-real-exam.css'])check('Entfernt '+f,!fs.existsSync(path.join(root,f)));check('Manifest ohne Legacyfelder',!fs.readFileSync(path.join(root,'module-manifest.json'),'utf8').includes('legacyTechnicalKeys')&&!fs.readFileSync(path.join(root,'module-manifest.json'),'utf8').includes('legacyId'));const failed=checks.filter(x=>!x.ok);console.log(`G54.48.4 ${checks.length-failed.length}/${checks.length}`);if(failed.length)process.exit(1);
