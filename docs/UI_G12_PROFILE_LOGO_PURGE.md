# UI-G12 Profile & Logo Purge

- Altes `#profileSetup` aus `index.html` entfernt.
- Renderer entfernt eventuell vorhandene alte Profilelemente beim Start.
- Brand-Logo wird inline als SVG gerendert, transparent und ohne Bild-/Cache-Artefakte.
- Bottom Dock wurde nicht neu gepatcht.
