# V8.3.2 Answer Engine Fix

Scope: keine UI-Neugestaltung, nur Funktionsfix für Spezialantwortmodelle.

## Behoben
- Bus Route Memory zeigt nach Ablauf der Animation automatisch die Sequenz-Auswahl.
- Route Memory nutzt nun sichtbar: auswählen, abwählen, letzte entfernen, Auswahl leeren, Gesamtantwort werten.
- EDV-11-Fehler bleibt als Schema-Aufgabe erhalten, hat aber zusätzlich eine sichtbare Multi-Select-Buttonliste.
- EDV-Auswahl kann über Schema-Karten oder Antwortbuttons erfolgen.
- Bewertung prüft weiterhin exakt 11 Fehler und wertet Teiltreffer sauber aus.

## Kontrolle
- JS-Syntax geprüft.
- ZIP-Test geprüft.
- Keine UI-Struktur geändert.
- Aufgabenbank nicht verändert.
