# UI-F2.4 iPad Hitbox & Close Fix

Umgesetzt:

- Router scannt jetzt echte Element-Rects statt nur `elementFromPoint`.
- Close-/X-Buttons werden vom Premium-Router nicht mehr blockiert.
- Touch/iPad-Karten bekommen stabile Hitboxes ohne Transform-Verschiebung.
- Dekorative Layer bleiben ohne Pointer-Events.
- Admin, Coach, Passwortsystem und Aufgabenbank bleiben unverändert.
