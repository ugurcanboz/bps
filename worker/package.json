{
  "name": "assessments-trainer-groq-worker",
  "version": "54.47.15-h",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js",
    "test:resilience": "node --test test/resilience-load.test.js",
    "load:live": "node tools/live-load-test.mjs",
    "deploy:beta": "npm run predeploy:beta && wrangler deploy --env beta",
    "deploy:production": "npm run predeploy:production && wrangler deploy --env production",
    "predeploy:beta": "node tools/predeploy-guard.mjs beta",
    "predeploy:production": "node tools/predeploy-guard.mjs production"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
